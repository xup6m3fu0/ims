{
  "name": "ims",
  "version": "1.0.0",
  "private": true,
  "description": "Inventory Management System (React + SQLite)",
  "scripts": {
    "dev": "npx concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "build:prod": "npm run build -w backend && npm run build -w frontend && mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "start": "npm run start -w backend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
